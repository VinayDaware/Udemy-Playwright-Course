export class User {
    constructor(firstName, lastName, age) {
        this.firstName = firstName;
        this.lastNameage = lastName;
        this.age = this.age;
    }
    getDetails() {
        return `${this.firstName} ${this.lastName}is ${this.age} is years old`;
    }

}